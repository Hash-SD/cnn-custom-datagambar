# Example secrets file for Streamlit Cloud
# Copy this to .streamlit/secrets.toml and fill in your values
# Note: secrets.toml is in .gitignore for security

# Model configuration (optional)
# MODEL_URL = "https://your-model-url.com/model.keras"

# API keys (if needed in future)
# API_KEY = "your-api-key-here"
